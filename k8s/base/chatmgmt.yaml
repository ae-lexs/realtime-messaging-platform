apiVersion: apps/v1
kind: Deployment
metadata:
  name: chatmgmt
spec:
  replicas: 1
  selector:
    matchLabels:
      app: chatmgmt
  template:
    metadata:
      labels:
        app: chatmgmt
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: chatmgmt
          image: messaging-chatmgmt
          ports:
            - name: http
              containerPort: 8083
          env:
            - name: ENVIRONMENT
              value: dev
            - name: CHATMGMT_HTTP_PORT
              value: "8083"
          resources:
            requests:
              cpu: 250m
              memory: 512Mi
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
          readinessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 3
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 5
            periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: chatmgmt
spec:
  type: ClusterIP
  selector:
    app: chatmgmt
  ports:
    - name: http
      port: 80
      targetPort: http
