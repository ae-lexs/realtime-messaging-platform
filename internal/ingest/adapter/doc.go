// Package adapter contains implementations of interfaces defined in app.
//
// Empty until Module 2, which lands the Cloud SQL write path (M2.2) and the
// outbox relay's Managed Kafka producer (M2.3) — ADR-023, ADR-022.
package adapter
